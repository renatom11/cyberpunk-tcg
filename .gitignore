__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
out/
*.jsonl.gz
# ...except the committed bootstrap sample: one real experience file so the schema has an example
# in the repo and tests/learn/test_harvest.py can fail loudly when a ruling changes its digest.
!data/experience/*.jsonl.gz
data/decks/generated/
site/
