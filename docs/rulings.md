# Rulings

The official guide leaves some situations unsettled; the Comprehensive Rules (last updated Sep 1, 2026) settled most of them and are cited below as "CR x.y". Rather than guess silently in code, each is
recorded here with a **default** and a corresponding field in `RulesConfig`. The active config is
hashed into every simulation result and replay, so changing a ruling visibly invalidates
comparisons with older runs instead of quietly shifting them.

**The official FAQ now outranks everything below.** `data/faq.txt` holds the published card and
general FAQs verbatim (269 answers over 140 of the 151 cards, plus 25 keyword rulings), and where it
speaks it settles the question outright — not by argument, by the publisher saying so. Three rulings
recorded here as **Uncertain** were decided that way, and are marked *Settled (FAQ)* below. Anything
the FAQ does not reach still falls back to the Comprehensive Rules, as before.

That source was first captured by pasting the page as text, which lost every keyword **icon** and
left 69 of the 269 answers reading with a gap where a symbol was; those entries were flagged
`icons_missing` and used as authority for nothing. They are no longer missing.
`tools/faq_from_pdf.py` rebuilds `data/faq.txt` from the published PDF, where each icon is a filled
vector chip whose colour and width identify it exactly, so the symbols are *measured* rather than
guessed — the whole objection to filling them in by hand. `icons_missing` now reads 0 and the flag
stays as a guard. The citations below are quoted with their icons; where one was previously quoted
in brackets because the symbol was unknown, the bracket is gone and the real icon is in its place.

Status legend — **Settled**: the guide implies it strongly enough that the alternative breaks the
game. **Settled (FAQ)**: the published FAQ answers it directly. **Uncertain**: a real coin-flip;
revisit when official clarification lands.

| # | Question | Default | `RulesConfig` | Status |
|---|---|---|---|---|
| 001 | Do Eddies ready during Start Phase step 1? | **Yes.** Step 1 says "return all your spent cards to ready", and Eddies are cards that spend to pay costs. Without this the economy has no engine — you could never spend the same Eddie twice. | `eddies_ready` = true | Settled |
| 002 | Are the identities of cards in an Eddies area public? | **Yes, as an unordered multiset.** Selling requires revealing the card; both players saw it. Order is lost because they sit face-down. | `perfect_eddie_memory` = true | Settled |
| 003 | Can a Legend spent as an Eddie still be Called? | **Yes.** Spent/ready and face-down/face-up are orthogonal states. | `spent_legend_callable` = true | Settled |
| 004 | Start Phase step 3 with an empty fixer area (turn 7+). | **Skip the step.** You may not take dice from your own Gig area, and stolen dice never enter a fixer area. | `empty_fixer_skips` = true | Settled |
| 005 | Are stolen dice rerolled? | **No** — the die keeps its face value, so stealing transfers Street Cred as well as a Gig. | `reroll_stolen_dice` = false | Settled |
| 006 | How is Overtime won? | **7 or more Gigs in your Gig area at any point** — checked continuously. The gameplay guide's "majority" wording was superseded by Comprehensive Rules 1.11. | `overtime_win` = "seven_gigs" | Settled (CR 1.11) |
| 007 | When exactly does Overtime begin? | **At the end of a turn, once both players have begun a turn with an empty fixer area** (CR 1.11.1, 8.17) — normally after 7 turns each. | `overtime_start` = "empty_fixers" | Settled (CR 1.11.1) |
| 008 | 7-Gig win vs. deckout on the same turn. | **The win check is Start Phase step 0, before the draw** — you win. | `win_check_before_draw` = true | Settled |
| 009 | May you attack a Gig area holding 0 dice? | **No** — an empty Gig area is not a valid attack target (CR 9.3.2.2). If it empties after the declaration the attack proceeds and steals nothing (CR 5.12.4.1, 9.22). | `allow_empty_gig_attack` = false | Settled (CR 9.3.2.2) |
| 010 | Do 0-power Units fight? | **Yes**, and they lose every fight they don't tie — but **a 0-power Unit cannot defeat a Unit** as the result of a fight (CR 9.19.2). Ties: both lose and both are defeated (CR 9.17.3). | `zero_power_fights` = true, `zero_power_cannot_defeat` = true | Settled (CR 9.17-9.19) |
| 011 | How many reactions per attack? | **As many as the defender wants**, in any order (CR 9.7); each BLOCKER fully replaces the current target (CR 9.9.1). | `max_redirects_per_attack` = 99 | Settled (CR 9.7) |
| 012 | Can a Blocker redirect an attack aimed at a spent Unit, or only at a Gig area? | **Both**, and the FAQ confirms it as well as the absence of a redirect limit — the engine already matches on both counts, so this is a confirmation and not a change. La Llorona *Ghost of the Past* answers it directly: *"Can I use a different Unit's BLOCKER effect after I block with La Llorna to redirect the attack again? **Yes. There is no limit of how many times you can use BLOCKER in one turn.**"* The general FAQ says the same for both target kinds (attacking the Gig area, and attacking another Unit — **Yes** to each), and that a ready Unit being attacked may **not** block for itself. `max_redirects_per_attack` is already 99 and `blocker_redirects_unit_attacks` already true. | `blocker_redirects_unit_attacks` = true | Settled (FAQ) |
| 013 | Must a Blocker be ready to redirect? | **Yes** — "spend this Unit" requires a ready card. | *(implied by spend)* | Settled |
| 014 | Can a lagged Unit use BLOCKER? | **Yes.** Lag only forbids attacking and activating ⊡ effects (CR 11.3.1); BLOCKER is a reaction on a ready Unit (CR 9.9). | `lagged_units_can_block` = true | Settled (CR 11.3.1) |
| 015 | A `GO SOLO` Legend on the field — still spendable as an Eddie? Does it hold its Legend slot? | **Not spendable** (it is a Unit now); its slot sits empty, and when it leaves the field it is removed from the game — a permanent loss of that Eddie source. | `go_solo_vacates_slot` = true | Uncertain |
| 016 | Can Gear be moved after it is played? | **No**, unless a card says so. Gear on a Legend that Go Solos moves with it. | `gear_reequip` = false | Settled |
| 017 | Can Gear be attacked? | **No** — only Units are legal attack targets. | — | Settled |
| 018 | Mulligan ordering and information. | **Simultaneous and hidden** — implemented sequentially, but neither player's view reveals the other's choice. | `hidden_mulligan` = true | Settled |
| 019 | "Once per turn" — does Calling a Legend on the Rival's turn consume your own turn's Call? | **No.** Both players' once-per-turn counters reset at the start of every turn, so a player may Call on their own turn and again on the Rival's. | `once_per_turn_scope` = "turn" | Uncertain |
| 020 | Hand size limit. | **None** — the guide states none. | `hand_limit` = null | Settled |
| 021 | May you Sell for Eddie during a reaction window? | **No** — main phase, your own turn only. | `sell_in_reactions` = false | Settled |
| 022 | Deck order semantics. | Index 0 is the **bottom**, index −1 the **top**. "Trash N" takes from the top and preserves order into the trash. Bottom-deck puts cards on the bottom in any order. | — | Settled |
| 023 | Do `ATTACK` triggers resolve before the target is declared? | **No** — the target is chosen as part of declaring the attack, then the attacker is spent and `ATTACK` / 'when spent' effects become pending together (CR 9.3-9.5, 11.21.2). The earlier reading of the gameplay guide was wrong. | `attack_triggers_before_target` = false | Settled (CR 9.3) |
| 024 | An attacker is spent after attacking — can the Rival attack it later? | **Yes.** Attacking carries a real cost. | — | Settled |
| 025 | Cost payment order when Eddies and Legends are both available. | Auto-paid for simulated play: ready Eddies → face-down Legends → face-up Legends (face-up last, since they may host Gear or have abilities). Exposing payment as an engine decision multiplies the search branching factor for almost no strategic content. A human at the board does choose, so the web client asks when there is more than one way to pay and passes the answer through `ops.PAY_PREF`; which of two face-down Eddies is spent changes nothing, since no card ever reads the identity of a card sitting in an Eddies area. | `explicit_payment` = false (AI); the web client overrides per action | Approximation |
| 026 | Is the field size-limited? | **No limit.** The online sim draws a fixed row of field slots, but the guide states no cap. | `field_limit` = null | Uncertain |
| 027 | Can a spent Legend use GO SOLO, and may it pay toward its own cost? | **Yes to both, and the FAQ corrects the second half of the old answer.** This row previously read "it may not spend itself toward its own cost", derived from CR 5.7.2.2; the general FAQ says otherwise — *"Can I spend a Legend for an Eddie when playing it with it's own GO SOLO? **Yes**"*. A Legend is therefore one of its own payment sources, exactly as `ops.payable_sources` already computes (ready; face-up with a Sell Tag, or face-down), and the engine's `exclude=` on the three `pay()` call sites was the only thing preventing it. Orientation on arrival is the other half and belongs to **047**, which is a different action: GO SOLO's own reminder text says it is played *as a ready Unit*, while paying a Legend's cost without the keyword preserves orientation and adds Lag. A spent Legend may still GO SOLO. **Implemented 2026-09-20**: `exclude=` is gone from the three *playing* `pay()` sites and from the two menu subtractions, and a solo'd Legend now arrives ready. `engine.activate` keeps its exclusion — an ability whose ⊡ cost spends the card cannot also spend it for €$, which is a different rule. The arrival half is **hard-coded rather than flagged**: every `RulesConfig` field is hashed into `digest()`, which `Replay.load`, `experience.read_games`, `learn.model` and the delayed suite all *enforce*, so a new field refuses the shipped weights (fitted on 80,000 games whose harvest no longer exists), the committed experience corpus, the demo replay and the tactics suite at once. A flag is for a ruling that might still be switched; this one is settled. `go_solo_requires_ready` keeps its own, separate job: whether a **spent** Legend may GO SOLO at all, which the FAQ answers *Yes*. | `go_solo_requires_ready` = false (legality only), `go_solo_enters_lagged` = true; arrival hard-coded ready | Settled (FAQ), implemented |
| 028 | Does the d20 winner choose, or simply go first? | **They choose** (the online sim offers "Go first / Go second"). | `first_player_choice` = true | Settled |
| 029 | Can power go below 0? | **No** — effective power floors at 0. Cards like Towerfall ("-5 power, then bottom-deck rival Units with power 0") only make sense this way. | — | Settled |
| 030 | What does ⊡ spend on a Gear ability? | **The Gear itself** — the glossary says the symbol means "spend this card". The host keeps its own ready state. | — | Uncertain |
| 031 | Are face-down Legends' texts active? | **No** — only face-up Legends contribute static effects, event triggers and abilities. Their identity is unknown, so their text can't apply. | — | Settled |
| 032 | Does a Legend's `PLAY` trigger when it GOES SOLO? | **Yes** — GO SOLO reads "pay this Legend's cost to **play it** as a ready Unit", and Adam Smasher *Ender of Legends* (GO SOLO + "PLAY: Defeat a rival Unit") only makes sense this way. Calling a Legend is not playing it. | — | Settled |
| 033 | Which Legends can be played to the field? | Any Legend with a cost (CR 4.5); in this set every costed Legend carries GO SOLO. Null-cost Legends never can (CR 4.5.3). | — | Settled (CR 4.5) |
| 034 | What happens to a Legend that leaves the field or Legends area? | **Removed from the game**, whatever sent it there — trash, hand or deck are invalid areas for a Legend (CR 4.4.1). Its Gear goes where it was sent and stays there (CR 4.12.2). DEFEATED triggers still resolve, after the removal (CR 4.4.2). | `legends_removed_when_leaving` = true | Settled (CR 4.4) |
| 035 | Where is a Program while it resolves? | **Outside every area** — not in your trash yet (CR 4.14.2). So "add a Program from your trash" can't pick the Program being played. | `programs_resolve_outside_areas` = true | Settled (CR 4.14.2) |
| 036 | Does a Sell caused by an effect use up the once-per-turn Sell action? | **Yes** (CR 11.9.2.2). Effects may still Sell any number of cards (CR 5.8.4). | `effect_sell_uses_action` = true | Settled (CR 11.9.2.2) |
| 037 | Adjusting a Gig to a value not on its face, or to the value it already has. | **The effect fails** — no change (CR 6.4.4, 6.4.5). "Up to N" effects let you pick a legal amount. | `set_gig_off_face_fails` = true | Settled (CR 6.4) |
| 038 | Street Cred with no Gigs. | **Null** — not 0: neither even nor odd, and smaller than 0 when compared (CR 5.11.4, 11.2.3). | `null_cred` = true | Settled (CR 11.2.3) |
| 039 | Start Phase order. | Win check, then start-of-turn effects (removing "until your next turn" effects first), then Ready, Draw, roll in a Gig (CR 8.6). Lag is removed from **all** Units at the end of each turn (CR 11.3.2). | — | Settled (CR 8.6) |
| 040 | May a face-up Legend without a Sell Tag be spent for 1 €$? | **No** (CR 5.7.2.2); face-down Legends always may. Every Legend in this set has a Sell Tag. | — | Settled (CR 5.7.2.2) |
| 041 | Calling a Legend through an effect. | Still limited to once per turn: "Call a Legend may be used only once per turn even if a game action tells you to" (CR 5.7.3). | — | Settled (CR 5.7.3) |
| 042 | Goro Takemura *Losing His Way* reads "ATTACK: If all friendly Legends are face-up, this Unit gains +5 power this turn." What happens with **no** Legends in the Legends area? | **Settled by the owner, 2026-09-21: a solo'd Legend standing on the field counts as a face-up Legend, and if every Legend has gone solo and been removed from the game there are no Legends left to be face-up, so Goro gains nothing.** The engine reads "all friendly Legends" over the Legends area *and* the field (`EffectCtx.faceup_legends` / `all_legends`, Stage 0 E5), requires the set to be non-empty, and grants +5 only when every member is face-up. Previously escalated and undecided: the competing reading was that "all" over an empty set is vacuously true. Ruling 038 (Street Cred with no Gigs is *null*, not 0) pointed the same way as the owner's answer. `tests/cards/audit/test_a07.py` now pins the settled reading. | — | Settled (owner ruling) |
| 043 | May an effect that says "Spend a rival Unit" target a Unit that is **already spent**? | **Yes, as the engine has it** — an already-spent Unit is a legal target and the spend simply does nothing (`ops.spend` returns immediately). Ruling **013** points the other way for BLOCKER, where "spend this Unit" is a *cost* the defender pays and a cost that cannot be paid cannot be chosen; here it is an *effect* choosing a target, and the mainstream convention across trading card games is that a spend/tap effect may name an already-spent permanent. The card text is silent and the distinction between paying a cost and applying an effect is not one this set ever spells out. It matters on exactly one card: Memory Relapse also says "It can't ready until your next turn", so targeting a spent Unit with it is a real play — the rider lands on a Unit that was never actually spent. On Corporate Surveillance the difference is unobservable, which is why no test can separate the two readings there. Raised by the audit as S-SUSPICION (`docs/audit-unproven.md`, batch A01): the auditor wrote the scenario, watched it pass, and refused to freeze a guess into an xfail. | — | Uncertain |
| 044 | Is a Legend played to the field with **GO SOLO** a "Unit" for the purposes of printed card text? | **It is BOTH a Unit and a Legend.** Four FAQ entries agree, and each reaches a different part of the question. General: *"When a Legend uses GO SOLO is it still a Legend? **Yes**"* — so it does not stop being a Legend. Synapse Burnout: *"If I use this on a friendly Legend in that field area that is now a also a Unit, does the Legend count itself for a +1? **Yes**"* — the FAQ's own words are "now also a Unit", and it counts under both headings at once. MaxTac Suppression Team: a Legend played to the field with GO SOLO this turn **cannot** attack into it — so it is a Unit for a restriction written about Units. Jackie Welles *Mama's Favorite*: the effect works **in the field area as a Unit** *and* in the Legends area. This dissolves the objection recorded against the inclusive reading: Chrome Fang (Units) and Westbrook Netrunner (Legends) are not redundant, they **both** apply to a solo'd Legend, and the FAQ confirms each separately (Chrome Fang: *"apply to all rival Units? Yes"*; Westbrook: a Legend played to the field after it still cannot steal). Consequences: the seven audit findings held as strict-xfail — `AUD-6th-street-recruits-1`, `AUD-satori-sword-of-saburo-1`, `AUD-river-ward-detective-on-the-hunt-1`, `AUD-jackie-welles-mamas-favorite-1` and `-2`, `AUD-jackie-welles-pour-one-out-for-me-1`, `AUD-saburo-arasaka-stubborn-patriarch-1` — are unblocked, and the 6 `CardDef.type` gates against 37 zone-based checks are a defect with a **known correct side**: zone-based is right for "Unit", and Legend-ness is answered by the card, not the zone. **Implemented 2026-09-20**: all six are zone-based, all seven markers deleted, and the lint now pins `TYPE_GATED_UNIT` at **empty** — kept rather than deleted, because a new script reaching for `CardDef.type` to ask "is this a Unit" would be invisible in review. `EffectCtx.is_unit` is the one way to ask. Two cases it deliberately does not cover: `type is LEGEND` tests are legitimate and untouched, since Legend-ness *is* answered by the card; and a `("defeated", ...)` listener cannot read the zone at all, because `ops.defeat` moves the card out of play and `move` clears `F_GO_SOLO` before the event fires — so `defeat` answers it and carries the answer as the event's fifth element, which is what River Ward reads. Hand-built boards (`tests/conftest.board`, `learn.delayed`) now set `F_GO_SOLO` on a Legend placed in the field area, because that is the only way one can be there and a board without it describes a position no game reaches. | — | Settled (FAQ), implemented |
| 045 | May an ability with **no legal target** be activated? | **As the engine has it: no for a target, yes for a magnitude or a printed condition** — and the set contains a counterexample to the first half. Four abilities carry an availability guard: Hanako *Daughter of the Emperor* ("Swap a friendly Gig with a rival Gig" — needs a die on each side), Panam *Nomad Cavalry* ("Move a Gear from this Legend to an unequipped friendly Unit"), Overwatch — Panam's Gift ("Discard 1. Defeat a spent rival Unit…" — needs a card to discard) and Panam *Strength Through Family* (guarded by the once-per-turn Call rule rather than by a target). Against them stands **Padre — Man of the Cross**, which prints the same two-sided shape ("⊡: Set a player's Gig to the same value as another player's Gig"), carries no guard, and is offered with no dice on the board at all — spent, resolved, nothing happens. Ruling 037 leans the same way for Gig effects: an impossible one "fails — no change", i.e. it is activated. The distinction the engine now draws, after `AUD-alt-cunningham-soulkiller-architect-1` and `AUD-kerry-eurodyne-the-last-rockerboy-1` were fixed, is that a guard may not encode a *magnitude* (a discount of −0) or a condition the card prints *inside* the effect ("If you control a Gig with 8+ value, draw 2"), because those are outcomes rather than costs. It matters whenever something watches for the spend: NetWatch NetDriver draws when its host is spent, so a denied activation is a card the controller cannot draw. `AUD-hanako-arasaka-daughter-of-the-emperor-1` was filed on Hanako and **killed** in verification — the blind reader, never shown the finding, read her guard as the minimum for a swap to be performable rather than as an added condition. Left as the engine has it, with the Padre counterexample recorded rather than explained away. | — | Uncertain |
| 046 | When two of a player's cards trigger on the **same event**, who chooses the order they resolve in? | **The controller of the triggers chooses.** Three general FAQ entries say so, and between them they cover all three populations this ruling said any answer would have to reach: *"When I have mulitple ATTACK effects that activate and go into pending at the same time. Can I choose any order to resolve them? **Yes**"*; *"I have a Unit with ATTACK and a Unit with 'When a friendly Unit attacks. Draw 1'. Will I be able to choose the order to resolve them? **Yes**"*; and the same question with a **PLAY** trigger against *'When a friendly Legend is spent. Draw 1'* — **Yes**. (The icons in those three were unknown when this row was first written and were quoted as gaps; recovered from the PDF, they are ATTACK, ATTACK and PLAY, which widens the answer rather than narrowing it: it covers two different trigger words, not one.) The per-card FAQs agree where they touch it: Netwatch Netdriver and Zetatech Faceplate both answer *"You can choose the order you resolve these effects"* when the Gear's spend trigger meets the host's own trigger. Note the FAQs that go the other way and are *not* about simultaneity: Netwatch/Faceplate versus an **activated** ⊡ effect is ordered (*"After. Resolve the activated effect first"*), as is paying a cost (*"After. Play the card first"*); and MaxTac Squadron warns that an end-of-turn ready resolved first can invalidate its own effect — so the choice is over triggers that land together, not over a trigger and the action that caused it. Implementing it is a `core/**` change (G2): `ops.dispatch`, the separate `ops.push_trigger` queue and the `s.mods` listener list all need it; it rewrites the golden action streams (regeneration under the five-evidence protocol) and adds a decision point the search must carry. Measured at **5.6%** of dispatched events over 40 games. Reuse `steps.ReactionWindowStep`'s shape — re-push a menu until the player passes — and the existing `Pick` action; a new Action class would change `learn.policy.KINDS`, hence `action_feature_digest()`, and every fitted policy head would be rejected on load. **Implemented 2026-09-20** as `steps.OrderTriggersStep`, on the existing `Pick`, with the digest confirmed unmoved. `ops.dispatch` still calls hooks inline in the 96.8% of events where nobody has a choice; `ops.needs_ordering` diverts the rest to the step, which asks, resolves exactly one, and re-pushes with the remainder. **Distinct cards only**, and that restriction is measured: over 287,247 dispatched events, 4.8% matched two of one player's hooks, but a third of those were two or three copies of the *same* card (Rita Wheeler beside Rita Wheeler). Ordering two identical effects has no distinguishable branches, so asking would be a prompt that cannot matter and a branching factor the search pays for and learns nothing from; requiring two distinct cards takes it to 3.2%. In play that is **10.2 prompts per game, 7.2% of decisions**, 75% of them binary. Across players there is no choice — the turn player's resolve first, which is the order the hook list already has. | — | Settled (FAQ), implemented |
| 047 | Can a Legend be played to the field **without** using GO SOLO? | **Yes, if it has a numeric cost.** General FAQ: *"Can I play a Legend to the field from the Legends area without using GO SOLO? **Yes**, as long as the Legend has a numeric cost value, you may pay its cost to play it to the field. It enters the field **with lag**, and **in the same orientation** it was in the Legends area."* V *Corporate Exile* repeats it for a specific card. This is a **second action on the same eight cards**, not a rewording of GO SOLO, and the two resolve differently: GO SOLO plays it *as a ready Unit* and lets it attack that turn, while paying the cost plainly preserves orientation and adds Lag. That also retires the apparent CR 4.5.1-versus-11.25.1 conflict in **027** — the two rules describe the two actions, not one contested one. **The engine offers only GO SOLO**: `core/legal.main_menu` gates the Legend branch on `Keyword.GO_SOLO in d.keywords`, so the plain play is unreachable. All 8 costed Legends in the set carry GO SOLO and all 19 without a cost are excluded by the FAQ's own condition, so this adds a choice to 8 cards rather than enabling any new card. `go_solo_enters_lagged` already describes the Lag half. **Implemented 2026-09-20**, and *not* as a new Action class: `GoSolo` gains a `keyword: bool = True` field, so `learn.policy.KINDS` and `action_feature_digest()` are unchanged (measured before and after: `2ea962c9a03712d4` both times) and every fitted policy head still loads. `keyword=False` pays `play_cost(go_solo=False)` — the printed cost, because a "GO SOLO costs less" effect names the keyword — keeps the orientation it had, takes Lag, and does **not** get the keyword's permission to attack through it. That last part needed a new flag, `F_NO_SOLO_KEYWORD`: the card still *prints* GO SOLO either way, so `has_keyword` alone cannot tell the two plays apart. Both set `F_GO_SOLO`, which means "a Legend standing on the field" and is what removes it from the game when it leaves (CR 4.4.1). Reachable and taken: 32 keyword-less plays across 160 random games. | `GoSolo(inst, keyword=False)` | Settled (FAQ), implemented |
| 048 | Kiroshi Optics prints "(Equip to a Unit or friendly face-up Legend.)" — may "a Unit" be a **rival** Unit? | **No — settled by the owner, 2026-09-21: you can never play or equip a card onto another player's cards.** Kiroshi Optics equips only a friendly Unit or a friendly face-up Legend, which is what `legal.gear_hosts` already offers for every Gear; the engine was correct and nothing changed. Recorded because the parenthetical qualifies only the Legend with "friendly", which read in isolation could be taken to widen the Unit half. `tests/rules/test_stage0_engine.py::test_kiroshi_optics_hosts_are_friendly_only` pins it. | — | Settled (owner ruling) |
| 049 | "The first time X happens each turn" — is that counted by the card's own memory, or by the turn's events? | **By the turn's events — Settled (FAQ), Stage 0 E6.** Three answers say so: a Yorinobu *Steel Dragon* played after an ARASAKA Unit died this turn does not draw for the next death; a Yorinobu *Embracing Destruction* flipped after an ARASAKA attack does not draw for the next attack; a Jackie *Pour One Out For Me* flipped after a Blue card does not trigger on the next one. And Yorinobu *Steel Dragon* "counts itself" for its own death. `GameState.turn_events` logs every dispatched event of the turn and `EffectCtx.first_this_turn` reads it; the eight scripts printing the phrase (both Yorinobus, Jackie, Johnny Silverhand, Rita Wheeler, Gorilla Arms, Rogue Amendiares *Queen of the Afterlife*, Viktor Vektor *Drop Your Illusions*) use it, and `ops.defeat` hands a card its own "defeated" event. | — | Settled (FAQ) |
| 050 | Dying Night on a Unit named "V" that attacks and is defeated before the end of the turn — do the 2 Eddies still ready? | **Yes — Settled (FAQ), Stage 0 E4.** *"If this Gear is attached to a Unit named 'V' and the Unit attacks but is defeated before the end of the turn, can I still ready 2 Eddies? Yes."* The ATTACK trigger books the payout on the player through a listener that outlives the Gear; a host that survives is paid once. | — | Settled (FAQ) |
| 051 | Bootleg Black Sapphire Show sells the top card of the deck — does anyone see it? | **No — Settled (FAQ), Stage 0 E7.** *"Do I reveal or get to look at the card I am selling? No."* The sold card carries `F_FACEDOWN` in the Eddies area: `view.knows_identity` answers No for both seats, the owner's mask permutes it with their deck and the rival's with the rival's hand+deck pool, and the learning features count it among the undrawn. Ruling 002 (the Eddies area is public) still holds for every card sold from hand. | — | Settled (FAQ) |
| 052 | Does the reaction window open when the defender can only Pass? | **Yes — owner decision 2026-09-21 (Stage 0 E10), a modelling rule rather than a rules question.** The window used to be skipped then, and the skip was a tell no table has: its absence proved the defender held no affordable QUICK, no ready BLOCKER and no Call. It now opens regardless and a Pass is a recorded decision. One residual is kept deliberately: the window that *re-opens* after a Block (ruling 012) still closes silently with nothing further to react with, because the frozen heuristic's preview cannot see past a pending decision on the rival's turn and opening it made the heuristic stop blocking. | — | Settled (owner ruling) |
